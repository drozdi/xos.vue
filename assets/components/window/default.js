import drDefault from "../dr-re/default";

export default {
    ...drDefault,
    icons: () => (['close']),
    actions: () => ([]),
    title: null,
    autoClose: true,
    wmGroup: 'wm',
    wmSort: 1,
}